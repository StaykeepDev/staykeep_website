{
  "name": "staykeep-website",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/generate-og-image.mjs && astro check && astro build && node scripts/patch-csp-hashes.mjs",
    "build:nocheck": "node scripts/generate-og-image.mjs && astro build && node scripts/patch-csp-hashes.mjs",
    "preview": "npm run build && wrangler dev",
    "check": "astro check",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/react": "^7.0.0",
    "@fontsource/cormorant-garamond": "^5.3.0",
    "@fontsource/plus-jakarta-sans": "^5.3.0",
    "astro": "^7.3.4",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@cloudflare/workers-types": "^5.20260924.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "sharp": "^0.35.4",
    "typescript": "^6.0.3",
    "wrangler": "^4.137.0"
  }
}
